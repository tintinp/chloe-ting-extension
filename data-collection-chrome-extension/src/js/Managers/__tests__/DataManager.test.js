describe('DataManager', () => {
  beforeEach(() => {
    jest.resetModules()
    jest.clearAllMocks()
  })

  // ------------------------------------------------------------------
  // Tests
  // ------------------------------------------------------------------
  test('Test data extraction', () => {
    expect(true).toStrictEqual(true)
  })
})
